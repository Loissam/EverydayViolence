import React from 'react'

import { Col } from 'reactstrap'

const SideBar = ({ className }) => {
    return (
        <div className={`${className} d-none d-lg-block d-xl-block side-bar full-height`}>
            &nbsp;
        </div>
    )
}

export default SideBar
